<Window x:Class="CommunalServicesApp.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CommunalServicesApp.Windows"
        mc:Ignorable="d"
        Title='МУП "Юго-Запад"' Height="500" Width="700" Background="AliceBlue">
    <Grid>
        <StackPanel>
            <Image Source="/Images/icons1.png" Width="50" Height="50" HorizontalAlignment="Right"  Margin="0 20 40 0"/>
            <TextBlock Text="Авторизация" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="35"
                       FontFamily="Georgia"/>

            <StackPanel Orientation="Horizontal" Width="300" Margin="0 40 0 0">
                <Image Source="/Images/icons2.png" Width="20" Height="20" VerticalAlignment="Center" Margin="0 0 5 0"/>
                <TextBlock Text="Логин:" FontSize="20" VerticalAlignment="Center"/>

            </StackPanel>
            <TextBox Name="LoginTextBox" Height="30" Width="300" Margin="0 5 0 0"   
                     Background="Transparent"
                     BorderBrush="Black"
                     BorderThickness="0,0,0,1"
                     Foreground="Black"
                     Padding="5,10,5,5"
                     FontSize="14"/>

            <StackPanel Orientation="Horizontal" Width="300" Margin="0 40 0 0">
                <Image Source="/Images/icons3.png" Width="20" Height="20" VerticalAlignment="Center" Margin="0 0 5 0"/>
                <TextBlock Text="Пароль:" FontSize="20" HorizontalAlignment="Center" />

            </StackPanel>
            <TextBox Name="PasswordTextBox" Height="30" Width="300" Margin="0 5 0 0" 
                     Background="Transparent"
                     BorderBrush="Black"
                     BorderThickness="0,0,0,1"
                     Foreground="Black"
                     Padding="5,10,5,5"
                     FontSize="14"/>

            <Button Content="Войти" Height="30" Width="150" FontSize="20" Margin="0 50 0 0" Background="#FFECF0F3" Click="LoginButtonClick">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>



        </StackPanel>
    </Grid>
</Window>
